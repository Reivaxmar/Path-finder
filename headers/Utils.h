#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <vector>
#include <queue>
using namespace std;

// using Vector2i = Vector2<int>;

// template<typename T>
// struct Vector2 {
//     T x, y;
//     pair<T, T> toPair() {
//         return make_pair(x, y);
//     }
//     Vector2<T>(T _x, T _y) : x(_x), y(_y) {}

//     // template<typename A, B>
//     // Vector2<T> operator+(Vector2i& a, Vector2<T>)
// };

#endif